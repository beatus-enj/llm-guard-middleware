# 核心依赖（flask/requests/pyyaml/python-dotenv 通常已预装）
flask>=3.0.0
requests>=2.28.0
pyyaml>=6.0
python-dotenv>=1.0.0

# 测试
pytest>=7.0.0

# ML 模型（可选，ENABLE_ML_MODEL=true 时需要）
# pip install transformers torch
# 仅 CPU 推理可改用: pip install transformers torch --index-url https://download.pytorch.org/whl/cpu
