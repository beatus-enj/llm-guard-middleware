# LLM Guard Middleware 配置
# 复制为 .env 后修改

# 上游 llama.cpp 服务地址
UPSTREAM_URL=http://localhost:8080

# 中间件监听端口
PORT=8000
HOST=0.0.0.0

# 功能开关
ENABLE_RULE_ENGINE=true
ENABLE_ML_MODEL=false    # 改为 true 并安装 transformers 后启用 ML 检测

# ML 模型配置（ENABLE_ML_MODEL=true 时生效）
ML_MODEL_NAME=unitary/toxic-bert
ML_MODEL_CACHE_DIR=./model_cache
ML_THRESHOLD=0.7
ML_INJECTION_THRESHOLD=0.65

# 自定义安全回复
SAFE_REPLY_MESSAGE=很抱歉，您的请求包含不当内容，无法处理。如有疑问请联系管理员。

# 规则配置文件路径
RULES_CONFIG=./config/rules.yaml

# 上游请求超时（秒）
UPSTREAM_TIMEOUT=120.0
