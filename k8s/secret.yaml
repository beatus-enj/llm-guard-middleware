apiVersion: v1
kind: Secret
metadata:
  name: llm-guard-secret
  namespace: llm-guard
type: Opaque
stringData:
  SAFE_REPLY_MESSAGE: "很抱歉，您的请求包含不当内容，无法处理。"
  # 告警 Webhook（有则填）
  # ALERT_WEBHOOK_URL: "https://qyapi.weixin.qq.com/..."
  # ALERT_WEBHOOK_TYPE: "wecom"